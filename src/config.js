const DATA_FILE_PATH = 'data.txt'; 
const BASE_WS_URL = 'wss://ws.secretivesentry.com/pingvpn/v1/clients/';
const RECONNECT_DELAY_MS = 5000;

module.exports = {
    DATA_FILE_PATH,
    BASE_WS_URL,
    RECONNECT_DELAY_MS
};